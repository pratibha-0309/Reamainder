import time
from plyer import notification

if __name__ == "__main__":
    while True:
        notification.notify( title=" PLEASE DRINK WATER",message="A normal adult can excrete up to 20 liters per day"" and does"" not ""exceed three to four cups of water in an hour, because that's what a normal kidney can excrete at a time."

                         .format(app_icon="E:\icon.ico"),
                         timeout=3
    )
        time.sleep(6)
#time.sleep(60*60)
